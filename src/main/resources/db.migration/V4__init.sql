CREATE TABLE Appointment
(
    appId      VARCHAR(255) PRIMARY KEY,
    email      VARCHAR(255),
    docId      VARCHAR(255),
    docName    VARCHAR(255),
    docSpecial VARCHAR(255),
    status     VARCHAR(255),
    date       VARCHAR(255)
);
